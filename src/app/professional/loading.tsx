import RouteLoadingOverlay from "@/components/RouteLoadingOverlay";

export default function Loading() {
  return (
    <RouteLoadingOverlay
      variant="professional"
      title="Opening Professional Space"
      subtitle="Syncing projects, skills, and experience..."
    />
  );
}
